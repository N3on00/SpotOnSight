# Development Workflow

- Branch from `develop`
- Keep migrations incremental
- Remove experiments instead of carrying dead code forward
- Document structure changes in `docs/`
- Run backend and frontend verification before opening a PR
