# API Overview

The backend exposes three groups of endpoints:

- Generic CRUD routes for simple authenticated resources
- Authentication routes under `/auth`
- Product workflows under `/social`

Primary feature areas include user profiles, spots, favorites, follows, blocks, shares, support tickets, comments, and meetups.
